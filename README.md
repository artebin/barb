# barb
Quick and not-so-dirty command line tool with support of multi-line patterns.  
I needed a friendly command, I made it.  
It is written in Java, it could have been Python or Perl, whatever 🤷‍♂️.
The executable is about 70kB but requires a JVM installed.
